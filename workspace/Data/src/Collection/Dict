Dict
----
Implements the typeclass for a dictionary and additional functions.
----

import Category.Eq
import Category.Ord
import Category.Mappable
import Category.Monoid
import Collection.Set


---
A ''Collection'' which maps a key onto a value.

Keys are required to be unique. When a key-value pair ''(k,v)'' gets inserted, the dictionary will point to this value (thus old values get overwritten).

The syntactic sugar ''{k --> v}'' desugars to ''Dict k v''
---
cat Dict (k:Eq) v is Set k
	empty	: dict k v
	insert	: k -> v -> dict k v -> dict k v
	lookup	: k -> dict k v -> v
	head	: dict k v -> (k,v)
	tail	: dict k v -> dict k v

instance Mappable (Dict k)

instance Monoid (Dict k v)
