PubPrivAlgo
-----------
Generic algorithm implementation
-----------

import Aliasses
import Data.Bool
import Collection.Maybe

-- The type representing the _private_ key of a public-key encryption scheme
cat PrivateKey
	> True

-- The type representing the _public_ key of a public-key encryption scheme
cat PublicKey
	> True


cat Encrypted a	is a
	> True

-- The type representing a public-key encryption scheme, with fixed keys
cat PubPrivAlgo (privKey:PrivateKey) (pubKey:PublicKey)
	encrypt	: PubPrivAlgo privKey pubKey -> privKey -> Message -> Encrypted Message


cat RSA is PubPrivAlgo RSAPrivKey RSAPubKey
	> True


instance RSAPrivKey is PrivateKey
instance RSAPubKey is PublicKey


data RSAPrivKey	= A
data RSAPubKey	= B
